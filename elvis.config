[{elvis, [{config, #{
    dirs => ["src"],
    src_dirs => ["src"],
    filter => "*.erl",
    ruleset => erl_files,
    ignore => [rebar3_prettypr] %% Copied from OTP, doesn't yet follow our rules
}}]}].
